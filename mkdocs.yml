site_name: bulletin-fetcher
site_description: Documentation for bulletin-fetcher
site_url: https://diegoglezsu.github.io/bulletin-fetcher/
repo_url: https://github.com/diegoglezsu/bulletin-fetcher
repo_name: diegoglezsu/bulletin-fetcher

theme:
  name: material
  favicon: assets/logo.jpg
  palette:
    - scheme: default
      primary: blue
      accent: blue
      toggle:
        icon: material/weather-night
        name: Switch to dark mode
    - scheme: slate
      primary: blue
      accent: blue
      toggle:
        icon: material/weather-sunny
        name: Switch to light mode
  features:
    - navigation.tabs
    - navigation.sections
    - navigation.top
    - search.highlight
    - search.share

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          paths: [src]

nav:
  - index.md
  - getting-started.md
  - development.md
  - API Reference:
      - bulletin.doue: api/doue.md

markdown_extensions:
  - admonition
  - attr_list
  - md_in_html
  - pymdownx.highlight:
      anchor_linenums: true
  - pymdownx.superfences
