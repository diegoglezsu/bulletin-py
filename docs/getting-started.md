# Getting Started

## Installation

```bash
pip install bulletin-py
```

## Suggested workflow

1. Install the package.
2. Import the public API from `bulletin`.
3. Use the source module you need, such as `bulletin.doue`.
